package com.betterlife.antifragile.presentation.base

import com.betterlife.antifragile.presentation.util.CustomToolbar

interface ToolbarConfigurable {
    fun configureToolbar(toolbar: CustomToolbar)
}
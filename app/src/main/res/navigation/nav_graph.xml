<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/splashActivity">

    <activity
        android:id="@+id/splashActivity"
        android:name="com.betterlife.antifragile.presentation.ui.splash.SplashActivity"
        android:label="Splash" />
    <activity
        android:id="@+id/loginActivity"
        android:name="com.betterlife.antifragile.presentation.ui.login.LoginActivity"
        android:label="Login" />

    <activity
        android:id="@+id/mainActivity"
        android:name="com.betterlife.antifragile.presentation.ui.main.MainActivity"
        android:label="Main" />

</navigation>